# 当前实现状态与已知问题

最后更新日期：2026-04-11

## 1. 当前已实现

### 工作台

- 房态工作台主页面
- 院落切换
- 房态筛选
- 楼栋列表
- 结构管理编辑态
- 新增院落 / 楼栋 / 楼层 / 房间
- 删除房间
- 顶部区吸顶
- 结构管理写操作已从页面脚本抽离到 `src/pages/workbench/useWorkbenchStructure.js`
- 工作台页面脚本已完成一轮瘦身，页面主要保留交互与导航逻辑

### 楼栋详情

- 查看楼栋下楼层与房间
- 顶部区固定、内容区单独滚动
- 点空房直接进入办理入住
- 点非空房打开房间详情抽屉

### 房间详情

- 当前情况与历史入住双视图
- 时间轴展示历史入住情况
- 收房租
- 收水电
- 抄表生成杂费
- 灵活收费
- 退租
- 档案预览入口
- 房间账务汇总、抄表计算、附件上传、退租等写操作已抽到领域服务层
- 页面残留乱码已做一轮集中清理
- 当前情况页已重组为：
  - 房间概况
  - 当前租客
  - 租金收费
  - 附加收费
  - 收费明细
- 房间概况与当前租客已支持折叠
- 租金收费和附加收费已统一为表格式布局
- 收费弹层已统一为较轻的底部抽屉样式
- 历史入住已简化为时间轴卡片直出，不再展示历史操作记录

### 办理入住

- 录入租客信息
- 设置租期与周期
- 计算支付计划
- 模拟上传身份证与合同
- 入住落库前的状态流转已改为通过领域服务执行，减少页面直接改树
- 已改为与房间详情同一套 `当前情况 / 历史入住` 结构
- 当前情况页已支持房间概况、当前租客折叠
- 日期选择已改为自定义弹层
- 结束日期不再手选，按开始日期 + 月数自动推算
- 当前租客页不再输入身份证号，只保留姓名与手机号

### 账务

- 已改为历史流水页，不再突出待收视图
- 顶部展示累计实收、房租实收、水电实收、灵活实收
- 支持按类型筛选历史流水
- 支持按房号 / 租客搜索
- 支持凭证预览抽屉
- 支持跳转对应房间详情

### 我的

- 顶部个人信息卡已统一为深色头卡
- 首页菜单重做为双分组卡片布局
- 支持租客档案搜索与预览入口
- 支持默认水电模板设置
- 支持电子合同模板入口
- 支持提醒机制设置
- 支持导出页入口
- 支持重置 demo 数据
- 二级页中文文案已大幅清理

### 通用能力

- 原生小程序 tabBar
- 路由节流与防重入
- mock 数据本地持久化
- 抽屉组件
- 时间轴组件
- 头部高度与安全区读取已统一收口到 `src/utils/layout.js`
- 已新增领域模型与 API 映射基础：
  - `src/domain/rent-models.js`
  - `src/domain/rent-room-service.js`
  - `src/domain/rent-api-mappers.js`
- 已新增领域测试与编码扫描：
  - `tests/domain.test.js`
  - `scripts/verify-encoding.cjs`

## 2. 当前不是正式完成状态

当前仍是高保真前端原型，不是正式生产系统。

### 尚未完成

- 真正后端接口
- 真正数据库
- Cloudflare R2 真实上传
- JWT 鉴权
- Excel 真实导出
- 多租户模型
- 完整异常态和弱网处理

## 3. 已知问题

### 高优先级

- 仓库里仍有部分历史文件与 mock 文案存在乱码污染，重点已从主页面转移到其余旧页面和少量文档
- 房间详情主抽屉仍然偏大，适合继续拆分折叠卡头、收费表格和弹层子组件
- 办理入住页虽然已重构，但仍有继续压缩表单长度的空间

### 中优先级

- 构建链路仍依赖 WXSS 清理脚本
- 目前仍是本地持久化，不是正式后端落库
- 历史 mock 数据和部分场景文案仍需继续系统性清理

## 4. 当前推荐接手顺序

1. 阅读 `docs/PROJECT_OVERVIEW.md`
2. 阅读 `docs/PRODUCT_REQUIREMENTS.md`
3. 阅读 `docs/TECH_ARCHITECTURE.md`
4. 阅读 `docs/PROJECT_HISTORY.md`
5. 再进入这些核心文件：
   - `src/data/rentStore.js`
   - `src/domain/rent-models.js`
   - `src/domain/rent-room-service.js`
   - `src/components/RoomDetailSheet.vue`
   - `src/pages/workbench/index.vue`
   - `src/pages/room/checkin.vue`
   - `src/pages/bills/index.vue`
   - `src/pages/profile/index.vue`

## 5. 下一阶段建议任务

### 第一优先级

1. 按页面继续清理残留乱码和历史污染文案
2. 把房间详情与入住页里的折叠卡头抽成公共组件
3. 继续统一底部双按钮区和表格头部样式

### 第二优先级

1. 正式引入后端服务
2. 完成结构管理 API
3. 完成账单与租约 API
4. 接入 Cloudflare R2 上传

### 第三优先级

1. 引入登录与 JWT
2. 设计多租户模型
3. 做真实导出
